The Linux Arichtecture uses "ASK" concept, where the layers are in onion layer
Application -> Sheel -> Kernal -> Hardware

Power On systen
     |
    BIOS (Motherboard which loads the system)
     |
  GNU GRUB (It loads Kernel)
     |
Unbuntu will Load
     |
  Systemd (Deamon will run in backgroud and produce the id 1)
     |
Systemctl (it will start the process/controller)

5 Commands

1) pwd = check the present working directory
2) mkdir = make an directory / folder
3) whoami = Knows the whoami
4) sudo  = connect to super user / root user
5) mv = move the files and create the folder if it is not there







service restarts confidently
